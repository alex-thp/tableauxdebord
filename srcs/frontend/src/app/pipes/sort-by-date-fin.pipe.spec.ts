import { SortByDateFinPipe } from './sort-by-date-fin.pipe';

describe('SortByDateFinPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByDateFinPipe();
    expect(pipe).toBeTruthy();
  });
});
