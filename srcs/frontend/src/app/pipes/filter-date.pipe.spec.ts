import { FilterDatePipe } from '../dashboard-hierarchy/filter-date.pipe';

describe('FilterDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
