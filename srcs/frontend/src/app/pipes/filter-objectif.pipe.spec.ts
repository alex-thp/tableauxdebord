import { FilterObjectifPipe } from './filter-objectif.pipe';

describe('FilterObjectifPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObjectifPipe();
    expect(pipe).toBeTruthy();
  });
});
