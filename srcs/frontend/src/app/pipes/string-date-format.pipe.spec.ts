import { StringDateFormatPipe } from './string-date-format.pipe';

describe('StringDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new StringDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
