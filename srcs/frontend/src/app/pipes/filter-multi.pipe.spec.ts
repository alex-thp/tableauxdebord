import { FilterMultiPipe } from './filter-multi.pipe';

describe('FilterMultiPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMultiPipe();
    expect(pipe).toBeTruthy();
  });
});
